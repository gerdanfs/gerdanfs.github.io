baseURL = "http://gerdanfs.github.io/" #"https://example.com"
publishDir = "docs"  # FSG: This is needed for github.pages

# [en, tr, fr, pl, ...] determines default content language
defaultContentLanguage = "en"
# theme
theme = "DoIt"
# website title
title = "Fatih Serhat Gerdan"
# whether to use robots.txt
enableRobotsTXT = true
# whether to use git commit log
# enableGitInfo = true
# whether to use emoji code
enableEmoji = true

[languages]
  [languages.en]
    weight = 1
    # language code
    languageCode = "en"
    # language name
    languageName = "English"
    # default amount of posts in each pages
    paginate = 15
    # [UA-XXXXXXXX-X] google analytics code
    # googleAnalytics = "UA-217779143-1"
    # copyright description used only for seo schema
    copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License."
    # Menu config
    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "posts"
        # you can add extra information before the name (HTML format is supported), such as icons
        pre = ""
        # you can add extra information after the name (HTML format is supported), such as icons
        post = ""
        name = "Posts"
        url = "/posts/"
        # title will be shown when you hover on this menu link.
        title = ""
        weight = 1
      [[languages.en.menu.main]]
        identifier = "tags"
        pre = ""
        post = ""
        name = "Tags"
        url = "/tags/"
        title = ""
        weight = 2
      [[languages.en.menu.main]]
        identifier = "categories"
        pre = ""
        post = ""
        name = "Categories"
        url = "/categories/"
        title = ""
        weight = 3
      [[languages.en.menu.main]]
        identifier = "fields"
        pre = ""
        post = ""
        name = "Fields"
        url = "/fields/"
        title = ""
        weight = 4
      [[languages.en.menu.main]]
        identifier = "series"
        pre = ""
        post = ""
        name = "Series"
        url = "/series/"
        title = ""
        weight = 5
      [[languages.en.menu.main]]
        identifier = "training"
        pre = "⭐"
        post = ""
        name = "Training"
        url = "/training/"
        title = ""
        weight = 6
      [[languages.en.menu.main]]
        identifier = "reviews"
        pre = "🔎"
        post = ""
        name = "Reviews"
        url = "/reviews/"
        title = ""
        weight = 7
      #[[languages.en.menu.main]]
        #identifier = "paid_content"
        #pre = "💲"
        #post = ""
        #name = "Paid"
        #url = "/paid-content/"
        #title = ""
        #weight = 8
      [[languages.en.menu.main]]
        identifier = "etc"
        pre = "🤪"
        post = ""
        name = "etc."
        url = "/etc/"
        title = ""
        weight = 9
      [[languages.en.menu.main]]
        identifier = "about"
        pre = ""
        post = ""
        name = "About"
        url = "/about/"
        title = ""
        weight = 10
      [[languages.en.menu.main]]
        identifier = "github"
        pre = "<i class='fab fa-github fa-fw'></i>"
        post = ""
        name = ""
        url = "https://github.com/gerdanfs"
        title = "GitHub"
        weight = 11
      [[languages.en.menu.main]]
          identifier = "twitter"
          pre = "<i class='fab fa-twitter fa-fw'></i>"
          post = ""
          name = ""
          url = "https://twitter.com/gerdanfs"
          title = "Twitter"
          weight = 12
      #[[languages.en.menu.main]]
          #identifier = "youtube"
          #pre = "<i class='fab fa-youtube fa-fw'></i>"
          #post = ""
          #name = ""
          #url = ""
          #title = "Youtube"
          #weight = 13
    [languages.en.params]
      # site description
      description = "A blog featuring interesting in-depth posts about programming (python), data science, machine learning, philosophy and sociology. Let's grow together!"
      # site keywords
      keywords = ["Programming", "Python", "Data Science", "Machine Learning", "Philosophy", "Sociology", "Book Reviews", "Movie Reviews", "Static Sites", "Hugo"]
      # titles
      blogPostsTitle = "All Blog Posts in This Site"
      mediumPostsTitle = "My Medium Posts"
      langSuffix = "en"
      # App icon config
      [languages.en.params.app]
        # optional site title override for the app when added to an iOS home screen or Android launcher
        title = "FSG"
        # whether to omit favicon resource links
        noFavicon = false
        # modern SVG favicon to use in place of older style .png and .ico files
        svgFavicon = ""
        # Safari mask icon color
        iconColor = "#5bbad5"
        # Windows v8-10 tile color
        tileColor = "#da532c"
      # Search config
      [languages.en.params.search]
        enable = true
        # type of search engine ("lunr", "algolia", "fuse")
        type = "fuse"
        # max index length of the chunked content
        contentLength = 4000
        # placeholder of the search bar
        placeholder = ""
        # max number of results length
        maxResultLength = 10
        # snippet length of the result
        snippetLength = 30
        # HTML tag name of the highlight part in results
        highlightTag = "em"
        # whether to use the absolute URL based on the baseURL in search index
        absoluteURL = false
        [languages.en.params.search.fuse]
          # https://fusejs.io/api/options.html
          isCaseSensitive = false
          minMatchCharLength = 2
          findAllMatches = false
          location = 0
          threshold = 0.3
          distance = 100
          ignoreLocation = false
          useExtendedSearch = false
          ignoreFieldNorm = false
      # Home page config
      [languages.en.params.home]
        # amount of RSS pages
        rss = 15

        # Home page profile
        [languages.en.params.home.profile]
          enable = true
          # URL of avatar shown in home page
          avatarURL = "/my-photo.png"
          # title shown in home page (HTML format is supported)
          title = ""
          # subtitle shown in home page
          subtitle = "Major in Philosophy, Minor in Sociology,<br />Programming & Data Science Enthusiast"
          # whether to use typeit animation for subtitle
          typeit = true
          # whether to show social links
          social = true
          # disclaimer (HTML format is supported)
          disclaimer = ""
        # Home page posts
        [languages.en.params.home.posts]
          # enable = false
          # special amount of posts in each home posts page
          # paginate = 5
      # Social config in home page
      [languages.en.params.social]
        GitHub = "gerdanfs"
        Twitter = "gerdanfs"
        Medium = "@gerdanfs"
        #Youtubechannel = "gerdanfs"
        BuyMeACoffee = "gerdanfs"
        Email = "gerdanfs@gmail.com"
        Devto = ""
        Gitlab = ""
        Bitbucket = ""
        Linkedin = ""
        Quora = ""
        Stackoverflow = ""
        Pinterest = ""
        Reddit = ""
        Codepen = ""
        FreeCodeCamp = ""
        Soundcloud = ""
        Spotify = ""
        Goodreads = ""
        Lastfm = ""
        Hackernews = ""
        Kickstarter = ""
        Patreon = ""
        Steam = ""
        Twitch = ""
        Googlescholar = ""
        Ko-Fi = ""
        Whatsapp = ""
        Instagram = ""
        Facebook = ""
        Telegram = ""
        VK = ""
        Flickr = ""
        Xing = ""
        Snapchat = ""
        Bandcamp = ""
        Paypal = ""
        Fivehundredpx = ""
        Mix = ""
        Foursquare = ""
        Strava = ""
        Skype = ""
        Zhihu = ""
        Douban = ""
        Angellist = ""
        Slidershare = ""
        Jsfiddle = ""
        Deviantart = ""
        Behance = ""
        Dribbble = ""
        Vine = ""
        Researchgate = ""
        Thingiverse = ""
        Wordpress = ""
        Tumblr = ""
        Gitea = ""
        XMPP = ""
        Matrix = ""
        Bilibili = ""
        ORCID = ""
        QQ = ""
        QQGroup = ""
        Liberapay = ""
        Linktree = ""
        Keybase = ""
        Weibo = ""
        Odnoklassniki = "" 
        RSS = true
      # Sponsor config
      [languages.en.params.sponsor]
        enable = true
        bio = "If you find this post helpful, please consider sponsoring."
        # link = "https://github.com/sponsors/gerdanfs"
        custom = "<a href=\"https://www.buymeacoffee.com/gerdanfs\" target=\"_blank\"><img src=\"https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png\" alt=\"Buy Me A Coffee\" style=\"height: 40px !important;width: 145 !important;\" ></a>"

  [languages.tr]
      weight = 2
      # language code
      languageCode = "tr"
      # language name
      languageName = "Türkçe"
      # default amount of posts in each pages
      paginate = 12
      # [UA-XXXXXXXX-X] google analytics code
      # googleAnalytics = "UA-217779143-1"
      # copyright description used only for seo schema
      copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License."
      # Menu config
      [languages.tr.menu]
        [[languages.tr.menu.main]]
          identifier = "posts"
          # you can add extra information before the name (HTML format is supported), such as icons
          pre = ""
          # you can add extra information after the name (HTML format is supported), such as icons
          post = ""
          name = "Yazı"
          url = "/posts/"
          # title will be shown when you hover on this menu link.
          title = ""
          weight = 1
        [[languages.tr.menu.main]]
        identifier = "tags"
        pre = ""
        post = ""
        name = "Etiket"
        url = "/tags/"
        title = ""
        weight = 2
        [[languages.tr.menu.main]]
          identifier = "categories"
          pre = ""
          post = ""
          name = "Kategori"
          url = "/categories/"
          title = ""
          weight = 3
        [[languages.tr.menu.main]]
          identifier = "fields"
          pre = ""
          post = ""
          name = "Alan"
          url = "/fields/"
          title = ""
          weight = 4
        [[languages.tr.menu.main]]
          identifier = "series"
          pre = ""
          post = ""
          name = "Seri"
          url = "/series/"
          title = ""
          weight = 5
        [[languages.tr.menu.main]]
          identifier = "training"
          pre = "⭐"
          post = ""
          name = "Alıştırma"
          url = "/training/"
          title = ""
          weight = 6
        [[languages.tr.menu.main]]
          identifier = "reviews"
          pre = "🔎"
          post = ""
          name = "İnceleme"
          url = "/reviews/"
          title = ""
          weight = 7
        #[[languages.tr.menu.main]]
          #identifier = "paid_content"
          #pre = "💲"
          #post = ""
          #name = "Ücretli"
          #url = "/paid-content/"
          #title = ""
          #weight = 8
        [[languages.tr.menu.main]]
          identifier = "etc"
          pre = "🤪"
          post = ""
          name = "vs."
          url = "/etc/"
          title = ""
          weight = 9
        [[languages.tr.menu.main]]
          identifier = "about"
          pre = ""
          post = ""
          name = "Hakkında"
          url = "/about/"
          title = ""
          weight = 10
        [[languages.tr.menu.main]]
          identifier = "github"
          pre = "<i class='fab fa-github fa-fw'></i>"
          post = ""
          name = ""
          url = "https://github.com/gerdanfs"
          title = "GitHub"
          weight = 11
        [[languages.tr.menu.main]]
          identifier = "twitter"
          pre = "<i class='fab fa-twitter fa-fw'></i>"
          post = ""
          name = ""
          url = "https://twitter.com/gerdanfs"
          title = "Twitter"
          weight = 12
        #[[languages.tr.menu.main]]
          #identifier = "youtube"
          #pre = "<i class='fab fa-youtube fa-fw'></i>"
          #post = ""
          #name = ""
          #url = "https://www.youtube.com/channel/gerdanfs"
          #title = "Youtube"
          #weight = 12
        
      [languages.tr.params]
        # site description
        description = "Programlama (python), veri bilimi, makine öğrenmesi, felsefe ve sosyoloji üzerine ilginç, derinlemesine yazılar içeren bir blog. Gelin beraber büyüyelim!"
        # site keywords
        keywords = ["Programlama", "Python", "Veri Bilimi", "Makine Öğrenmesi", "Felsefe", "Sosyoloji", "Kitap İncelemeleri", "Film İncelemeleri", "Statik Siteler", "Hugo"]
        # titles
        blogPostsTitle = "Buradaki Bütün Yazılar"
        mediumPostsTitle = "Medium'daki Yazılarım"
        langSuffix = "tr"
        # App icon config
        [languages.tr.params.app]
          # optional site title override for the app when added to an iOS home screen or Android launcher
          title = "FSG"
          # whether to omit favicon resource links
          noFavicon = false
          # modern SVG favicon to use in place of older style .png and .ico files
          svgFavicon = ""
          # Safari mask icon color
          iconColor = "#5bbad5"
          # Windows v8-10 tile color
          tileColor = "#da532c"
        # Search config
        [languages.tr.params.search]
          enable = true
          # type of search engine ("lunr", "algolia", "fuse")
          type = "fuse"
          # max index length of the chunked content
          contentLength = 4000
          # placeholder of the search bar
          placeholder = ""
          # max number of results length
          maxResultLength = 10
          # snippet length of the result
          snippetLength = 30
          # HTML tag name of the highlight part in results
          highlightTag = "em"
          # whether to use the absolute URL based on the baseURL in search index
          absoluteURL = false
          [languages.tr.params.search.fuse]
            # https://fusejs.io/api/options.html
            isCaseSensitive = false
            minMatchCharLength = 2
            findAllMatches = false
            location = 0
            threshold = 0.3
            distance = 100
            ignoreLocation = false
            useExtendedSearch = false
            ignoreFieldNorm = false
        # Home page config
        [languages.tr.params.home]
          # amount of RSS pages
          rss = 15
          # Home page profile
          [languages.tr.params.home.profile]
            enable = true
            # URL of avatar shown in home page
            avatarURL = "/my-photo.png"
            # title shown in home page (HTML format is supported)
            title = ""
            # subtitle shown in home page
            subtitle = "Felsefe ve Sosyoloji (Yandal) Mezunu,<br />Programlama ve Veri Bilimi Meraklısı"
            # whether to use typeit animation for subtitle
            typeit = true
            # whether to show social links
            social = true
            # disclaimer (HTML format is supported)
            disclaimer = ""
          # Home page posts
          [languages.tr.params.home.posts]
            enable = true
            # special amount of posts in each home posts page
            paginate = 7
        # Social config in home page
        [languages.tr.params.social]
          GitHub = "gerdanfs"
          Twitter = "gerdanfs"
          Medium = "@gerdanfs"
          #Youtubechannel = "gerdanfs"
          BuyMeACoffee = "gerdanfs"
          Email = "gerdanfs@gmail.com"
          RSS = true
        # Sponsor config
        [languages.tr.params.sponsor]
          enable = true
          bio = "Bu yazıyı faydalı bulduysanız, sponsor olup bana destek olabilirsiniz."
          # link = "https://github.com/sponsors/gerdanfs"
          custom = "<a href=\"https://www.buymeacoffee.com/gerdanfs\" target=\"_blank\"><img src=\"https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png\" alt=\"Buy Me A Coffee\" style=\"height: 40px !important;width: 145 !important;\" ></a>"

[params]
  # website title
  title = "Fatih Serhat Gerdan"
  # DoIt theme version
  version = "0.2.X"
  # site default theme ("light", "dark", "black", "auto")
  defaultTheme = "light"
  # public git repo url only then enableGitInfo is true
  gitRepo = "https://github.com/gerdanfs/gerdanfs.github.io"
  # which hash function used for SRI, when empty, no SRI is used ("sha256", "sha384", "sha512", "md5")
  fingerprint = ""
  # date format
  dateFormat = "2006-01-02"
  # website images for Open Graph and Twitter Cards
  images = ["/my-photo.png"]
  # enable PWA
  # enablePWA = true
  # license information
  license = '<a rel="license external nofollow noopener noreffer" href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank">CC BY-NC 4.0</a>'
  # [Experimental] Bundle js
  # bundle = true
  # Header config
  [params.header]
    # desktop header mode ("fixed", "normal", "auto")
    desktopMode = "fixed"
    # mobile header mode ("fixed", "normal", "auto")
    mobileMode = "auto"
    # theme change mode ("switch", "select")
    themeChangeMode = "select"
    # Header title config
    [params.header.title]
      # URL of the LOGO
      logo = ""
      # title name
      name = "FSG"
      # you can add extra information before the name (HTML format is supported), such as icons
      pre = "🏠"                       # "<i class='far fa-edit fa-fw'></i>"
      # you can add extra information after the name (HTML format is supported), such as icons
      post = ""
      # whether to use typeit animation for title name
      # typeit = true

  # Footer config
  [params.footer]
    enable = true
    # Custom content (HTML format is supported)
    custom = ""
    # whether to show Hugo and theme info
    hugo = true
    # Hosted on (HTML format is supported)
    # <a title="Github Pages" href="https://docs.github.com/en/pages/" target="_blank" rel="noopener noreffer">GitHub Pages</a>
    hostedOn = '<a title="Github Pages" href="https://docs.github.com/en/pages/" target="_blank" rel="noopener noreffer">GitHub Pages</a>' 
    # whether to show copyright info
    copyright = true
    # whether to show the author
    author = true
    # site creation time
    since = 2022
    # ICP info only in China (HTML format is supported)
    # icp = ""
    # license info (HTML format is supported)
    license= '<a rel="license external nofollow noopener noreffer" href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank">CC BY-NC 4.0</a>'

  # Section (all posts) page config
  [params.section]
    # special amount of posts in each section page
    paginate = 20
    # date format (month and day)
    dateFormat = "01-02" #"Monday, Jan 2, 2006"
    # amount of RSS pages
    rss = 15
    # recently updated posts settings
    [params.section.recentlyUpdated]
      enable = true
      rss = true
      days = 30
      maxCount = 10

  # List (category or tag) page config
  [params.list]
    # special amount of posts in each list page
    paginate = 20
    # date format (month and day)
    dateFormat = "01-02"
    # amount of RSS pages
    rss = 15

  # Page config
  [params.page]
    # whether to hide a page from home page
    hiddenFromHomePage = false
    # whether to hide a page from search results
    hiddenFromSearch = false
    # whether to enable twemoji
    twemoji = false
    # whether to enable lightgallery
    lightgallery = false
    # whether to enable the ruby extended syntax
    ruby = true
    # whether to enable the fraction extended syntax
    fraction = true
    # whether to enable the fontawesome extended syntax
    fontawesome = true
    # whether to show link to Raw Markdown content of the content
    linkToMarkdown = true
    # configure the link to view source the post
    # linkToSource = "https://github.com/HEIGE-PCloud/DoIt/blob/main/exampleSite/content/{path}"
    # "https://github.com/user/repo/blob/main/{path}"
    # configure the link to edit the post
    # linkToEdit = "https://github.com/HEIGE-PCloud/DoIt/edit/main/exampleSite/content/{path}"
    # https://github.com/user/repo/edit/main/{path}
    # https://gitlab.com/user/repo/-/edit/main/{path}
    # https://bitbucket.org/user/repo/src/main/{path}?mode=edit
    # configure the link to report issue for the post
    # linkToReport = "https://github.com/HEIGE-PCloud/DoIt/issues/new?title=[bug]%20{title}&body=|Field|Value|%0A|-|-|%0A|Title|{title}|%0A|Url|{url}|%0A|Filename|https://github.com/HEIGE-PCloud/DoIt/blob/main/exampleSite/content/{path}|"
    # https://github.com/user/repo/issues/new?title=[bug]%20{title}&body=|Field|Value|%0A|-|-|%0A|Title|{title}|%0A|Url|{url}|%0A|Filename|https://github.com/user/repo/blob/main/{path}|"
    # https://gitlab.com/user/repo/-/issues/new?issue[title]=[bug]%20{title}&issue[description]=|Field|Value|%0A|-|-|%0A|Title|{title}|%0A|Url|{url}|%0A|Filename|https://gitlab.com/user/repo/-/edit/main/{path}|
    # whether to show the full text content in RSS
    rssFullText = false
    # Page style ("normal", "wide")
    pageStyle = "wide"
    # whether to enable series navigation
    seriesNavigation = true
    # Display a message at the beginning of an article to warn the reader that its content might be outdated.
    [params.page.outdatedArticleReminder]
      enable = true
      # Display the reminder if the last modified time is more than 90 days ago.
      reminder = 365
      # Display warning if the last modified time is more than 180 days ago.
      warning = 750
    # Table of the contents config
    [params.page.toc]
      # whether to enable the table of the contents
      enable = true
      # whether to keep the static table of the contents in front of the post
      keepStatic = false
      # whether to make the table of the contents in the sidebar automatically collapsed
      auto = true
    # Code config
    [params.page.code]
      # whether to show the copy button of the code block
      copy = true
      # the maximum number of lines of displayed code by default
      maxShownLines = 10
    # Table config
    [params.page.table]
      # whether to enable sorting in the tables
      sort = true
    # KaTeX mathematical formulas config (KaTeX https://katex.org/)
    [params.page.math]
      enable = false
      # default block delimiter is $$ ... $$ and \\[ ... \\]
      blockLeftDelimiter = ""
      blockRightDelimiter = ""
      # default inline delimiter is $ ... $ and \\( ... \\)
      inlineLeftDelimiter = ""
      inlineRightDelimiter = ""
      # KaTeX extension copy_tex
      copyTex = true
      # KaTeX extension mhchem
      mhchem = true
    # Mapbox GL JS config (Mapbox GL JS https://docs.mapbox.com/mapbox-gl-js)
    [params.page.mapbox]
      # access token of Mapbox GL JS
      # accessToken = "pk.eyJ1IjoiZGlsbG9uenEiLCJhIjoiY2s2czd2M2x3MDA0NjNmcGxmcjVrZmc2cyJ9.aSjv2BNuZUfARvxRYjSVZQ"
      # style for the light theme
      # lightStyle = "mapbox://styles/mapbox/light-v10?optimize=true"
      # style for the dark theme
      # darkStyle = "mapbox://styles/mapbox/dark-v10?optimize=true"
      # whether to add NavigationControl (https://docs.mapbox.com/mapbox-gl-js/api/#navigationcontrol)
      # navigation = true
      # whether to add GeolocateControl (https://docs.mapbox.com/mapbox-gl-js/api/#geolocatecontrol)
      # geolocate = true
      # whether to add ScaleControl (https://docs.mapbox.com/mapbox-gl-js/api/#scalecontrol)
      # scale = true
      # whether to add FullscreenControl (https://docs.mapbox.com/mapbox-gl-js/api/#fullscreencontrol)
      # fullscreen = true
    # Social share links in post page
    [params.page.share]
      enable = true
      Twitter = true
      Reddit = true
      Facebook = true
      Linkedin = true
      Whatsapp = true
      Pinterest = true
      HackerNews = true
      Tumblr = true
      VK = true
      Buffer = false
      Xing = false
      Line = true
      Instapaper = false
      Pocket = true
      Digg = false
      Stumbleupon = true
      Flipboard = false
      Weibo = true
      Renren = false
      Myspace = false
      Blogger = false
      Baidu = false
      Odnoklassniki = false
      Evernote = true
      Skype = false
      Trello = true
      Mix = false
      Telegram = true
    # Comment config
    [params.page.comment]
      enable = true
      # giscus comment config (https://giscus.app/)
      [params.page.comment.giscus]
        enable = true
        # owner/repo
        dataRepo = "gerdanfs/gerdanfs.github.io"
        dataRepoId = "R_kgDOGptkgQ"
        dataCategory = "Announcements"
        dataCategoryId = "DIC_kwDOGptkgc4CAyA2"
        dataMapping = "pathname"
        dataReactionsEnabled = "1"
        dataEmitMetadata = "0"
        lightTheme = "light"
        darkTheme = "dark"
    # Third-party library config
    [params.page.library]
      [params.page.library.css]
        # someCSS = "some.css"
        # located in "assets/"
        # Or
        # someCSS = "https://cdn.example.com/some.css"
      [params.page.library.js]
        # someJavascript = "some.js"
        # located in "assets/"
        # Or
        # someJavascript = "https://cdn.example.com/some.js"
    # Page SEO config
    [params.page.seo]
      # image URL
      images = []
      # Publisher info
      [params.page.seo.publisher]
        name = ""
        logoUrl = ""

  # Sponsor config
  [params.sponsor]
    enable = true
    bio = "If you find this post helpful, please consider sponsoring."
    # link = "https://github.com/sponsors/gerdanfs"
    custom = "<a href=\"https://www.buymeacoffee.com/gerdanfs\" target=\"_blank\"><img src=\"https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png\" alt=\"Buy Me A Coffee\" style=\"height: 40px !important;width: 145 !important;\" ></a>"

  # TypeIt config
  [params.typeit]
    # typing speed between each step (measured in milliseconds)
    speed = 100
    # blinking speed of the cursor (measured in milliseconds)
    cursorSpeed = 1000
    # character used for the cursor (HTML format is supported)
    cursorChar = "|"
    # cursor duration after typing finishing (measured in milliseconds, "-1" means unlimited)
    duration = -1

  # Site verification code for Google/Bing/Yandex/Pinterest/Baidu
  [params.verification]
    google = "iRaOyXm7WHKrZmHVy0xJpUojeiIfiYTW6JQRd1MuUqg"
    bing = ""
    yandex = ""
    pinterest = ""
    baidu = ""
    so = ""
    sogou = ""

  # Site SEO config
  [params.seo]
    # image URL
    image = "/images/Apple-Devices-Preview.png"
    # thumbnail URL
    thumbnailUrl = "/images/screenshot.png"

  # Analytics config
  #[params.analytics]
    #enable = true
    # Google Analytics
    #[params.analytics.google]
      #id = "UA-217779143-1"
      # whether to anonymize IP
      #anonymizeIP = true

  # Cookie consent config
  [params.cookieconsent]
    enable = false
    # text strings used for Cookie consent banner
    [params.cookieconsent.content]
      message = ""
      dismiss = ""
      link = ""

  # CDN config for third-party library files
  [params.cdn]
    # CDN data file name, disabled by default
    # ("jsdelivr.yml")
    # located in "themes/DoIt/assets/data/cdn/" directory
    # you can store your own data files in the same path under your project:
    # "assets/data/cdn/"
    data = ""

  # Compatibility config
  [params.compatibility]
    # whether to use Polyfill.io to be compatible with older browsers
    polyfill = false
    # whether to use object-fit-images to be compatible with older browsers
    objectFit = false

# Markup related configuration in Hugo
[markup]
  # Syntax Highlighting (https://gohugo.io/content-management/syntax-highlighting)
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = true
    lineNumbersInTable = true
    # false is a necessary configuration (https://github.com/dillonzq/LoveIt/issues/158)
    noClasses = false
  # Goldmark is from Hugo 0.60 the default library used for Markdown
  [markup.goldmark]
    [markup.goldmark.extensions]
      definitionList = true
      footnote = true
      linkify = true
      strikethrough = true
      table = true
      taskList = true
      typographer = true
    [markup.goldmark.renderer]
      # whether to use HTML tags directly in the document
      unsafe = true
  # Table Of Contents settings
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 6

# Author config
[author]
  name = "Fatih Serhat Gerdan"
  email = "gerdanfs.gmail.com"
  #link = "https://github.com/gerdanfs"
  avatar = "/my-photo.png"

# Sitemap config
[sitemap]
  changefreq = "weekly"
  filename = "sitemap.xml"
  priority = 0.5

# Permalinks config (https://gohugo.io/content-management/urls/#permalinks)
[Permalinks]
  # posts = ":year/:month/:filename"
  posts = ":filename"
  # about = ":filename"

# Privacy config (https://gohugo.io/about/hugo-and-gdpr/)
[privacy]
  # privacy of the Google Analytics (replaced by params.analytics.google)
  #[privacy.googleAnalytics]
    # ...
  [privacy.twitter]
    enableDNT = true
  [privacy.youtube]
    privacyEnhanced = true

# Options to make output .md files
[mediaTypes]
  [mediaTypes."text/plain"]
    suffixes = ["md"]

# Options to make output .md files
[outputFormats.MarkDown]
  mediaType = "text/plain"
  isPlainText = true
  isHTML = false

# Options to make hugo output files
[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML", "MarkDown"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]
  taxonomyTerm = ["HTML"]

# Options for taxonomies
[taxonomies]
author = "authors"
category = "categories"
tag = "tags"
series = "series"
field = "fields"

##########################################
# FSG: My custom variables and parameters
[params.fsg]
  enableCustomDebugger = false

##########################################